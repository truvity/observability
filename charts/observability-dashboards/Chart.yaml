apiVersion: v2
name: observability-dashboards
description: >-
  The generic dashboard set, shipped as one ConfigMap per dashboard,
  labelled for Grafana's sidecar, in Grafana's own namespace — and the
  six-rule lint every dashboard here and every estate's own must pass.
type: application
# Stamped from the tag by the release workflow; never moved by hand.
version: 0.0.0
appVersion: "0.0.0"
keywords:
  - dashboards
  - grafana
  - victoriametrics
  - kubernetes
home: https://github.com/truvity/observability
sources:
  - https://github.com/truvity/observability
