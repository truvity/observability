# The log store's free-space metric without its limit: LogStoreDiskNearGuard
# would compare a gauge against nothing.
selfAlerts:
  diskGuard:
    logs:
      freeSpaceMetric: example_vl_free_disk_space_bytes
